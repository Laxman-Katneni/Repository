


void popTitles (int numBooks, char** books);  /* Function to populate book titles using pointers*/

char** popFavs (char** books, int numBooks, int numFavs); /* Function to populate favorite books. Returns a character double pointer with favorite book names. */

void toFile (char** books, int numBooks, char** favBooks, int numFavs); /* Function to save the file. */
